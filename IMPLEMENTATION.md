# IMPLEMENTATION.md — retired

This file tracked milestones M0–M8 and then the unbuilt-work list; both jobs are
done or moved:

- **What's left to build** → `STATUS.md` (the single backlog, kept current at
  every feature close-out).
- **How features get built** (strategy → implement → review) → `WORKFLOW.md`.
- **Why it's built this way** → `ARCHITECTURE.md`.
- **What shipped, per feature** → each feature's own doc (see the docs map in
  `CLAUDE.md`).

Kept as a stub so old links resolve. Don't add content here.

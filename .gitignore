# local assistant/session config
.claude/

# node / next (also covered by drift/.gitignore)
node_modules/
.next/

# secrets — never commit real keys
.env
.env.local
*.env.local

# design-sync machine state (durable inputs in .design-sync/ ARE committed)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# OS noise
.DS_Store
Thumbs.db
desktop.ini
